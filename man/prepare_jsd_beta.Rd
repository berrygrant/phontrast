% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_helpers.R
\name{prepare_jsd_beta}
\alias{prepare_jsd_beta}
\title{Prepare JSD estimates for beta regression / GAMs}
\usage{
prepare_jsd_beta(jsd_df, jsd_col = "jsd_mean", eps = 1e-06)
}
\arguments{
\item{jsd_df}{Data frame containing a JSD column.}

\item{jsd_col}{String: name of the JSD column (default "jsd_mean").}

\item{eps}{Small constant used to bound JSD away from 0 and 1.}
}
\value{
A modified data frame with an added \code{jsd_beta} column.
}
\description{
JSD lives in \eqn{[0,1]}. This helper adds a \code{jsd_beta} column bounded in
\eqn{(0,1)} so it can be used with the Beta family (e.g., in \pkg{mgcv}).
}
\examples{
jsd_by_speaker <- data.frame(
  speaker = paste0("s", 1:8),
  age = c(18, 22, 27, 31, 38, 45, 52, 60),
  jsd_mean = c(0.02, 0.05, 0.08, 0.13, 0.18, 0.24, 0.31, 0.39)
)

model_data <- prepare_jsd_beta(jsd_by_speaker)
model_data

if (requireNamespace("mgcv", quietly = TRUE)) {
  fit <- mgcv::gam(
    jsd_beta ~ age,
    data = model_data,
    family = mgcv::betar(),
    method = "REML"
  )
  stats::predict(fit, type = "response")
}
}
