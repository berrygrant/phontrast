% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jsd_summary.R
\name{jsd_summary}
\alias{jsd_summary}
\title{JSD summary: point estimate and optional bootstrap per group}
\usage{
jsd_summary(
  data,
  group_col,
  category_col,
  features,
  do_boot = TRUE,
  n_boot = 1000,
  min_tokens = 20,
  conf_level = 0.95,
  bw = c("Hpi", "Hscv", "Hpi.diag", "scott.diag"),
  eval_on = c("pooled", "group1", "group2", "pooled_sample"),
  eval_n = NULL,
  eval_seed = NULL,
  engine = c("ks", "fast_diag", "fast_diagonal"),
  chunk_size = 1000L,
  method = c("mc", "legacy"),
  ...
)
}
\arguments{
\item{data}{Data frame containing acoustic measurements.}

\item{group_col}{Character vector giving one or more grouping columns
(e.g., \code{"speaker"} or \code{c("Sex", "Style")}).}

\item{category_col}{String: name of column giving the category
to compare (e.g., "vowel"). Each group must have exactly two categories.}

\item{features}{Character vector of column names giving the acoustic space.}

\item{do_boot}{Logical; if TRUE (default), perform bootstrap via \code{boot_jsd()}.}

\item{n_boot}{Integer; number of bootstrap resamples per group if
\code{do_boot = TRUE}.}

\item{min_tokens}{Minimum number of tokens per group required to compute
JSD. Groups with fewer tokens are dropped.}

\item{conf_level}{Confidence level for bootstrap intervals.}

\item{bw}{Bandwidth selection method passed to \code{jsd_kde_nd()}.}

\item{eval_on}{KDE evaluation points passed to \code{jsd_kde_nd()}.}

\item{eval_n}{Optional maximum number of KDE evaluation points.}

\item{eval_seed}{Optional integer seed for KDE evaluation-point subsampling.}

\item{engine}{KDE evaluation engine passed to \code{jsd_kde_nd()}.
\code{"fast_diagonal"} is accepted as an alias for \code{"fast_diag"}.}

\item{chunk_size}{Chunk size for \code{engine = "fast_diag"}.}

\item{method}{Estimator passed to \code{jsd_kde_nd()}: \code{"mc"} (default)
or \code{"legacy"} (pre-1.2.0 self-normalized estimate).}

\item{...}{Additional arguments passed to \code{jsd_kde_nd()}.}
}
\value{
A tibble with one row per group and columns:
\itemize{
\item \code{group} – group ID (e.g., speaker)
\item \code{n_tokens} – number of tokens for that group
\item \code{jsd_point} – single JSD point estimate
\item \code{n_boot}, \code{conf_level}, \code{jsd_mean},
\code{jsd_sd}, \code{ci_lower}, \code{ci_upper},
\code{jsd_low}, \code{jsd_high} – bootstrap summary columns.
These are \code{NA} (or 0 for \code{n_boot}) if
\code{do_boot = FALSE}.
}
}
\description{
Convenience wrapper that returns both the point-estimate JSD and,
optionally, bootstrap-based uncertainty (mean, SD, and CI) for each group.
}
