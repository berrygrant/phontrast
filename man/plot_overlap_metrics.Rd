% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_overlap_metrics}
\alias{plot_overlap_metrics}
\title{Plot overlap metric comparisons}
\usage{
plot_overlap_metrics(
  metrics,
  value = c("separation", "estimate"),
  metric = NULL,
  group_col = NULL,
  show_ci = TRUE,
  facet = TRUE,
  sort = TRUE
)
}
\arguments{
\item{metrics}{Data frame returned by \code{phontrast()}.}

\item{value}{Scale to plot: \code{"separation"} plots
\code{separation_value}; \code{"estimate"} plots raw metric estimates.}

\item{metric}{Optional character vector of metric display names to include.}

\item{group_col}{Optional column to use on the x-axis. Defaults to
\code{"group"} when present, then \code{"scope"}.}

\item{show_ci}{Logical; if \code{TRUE}, draw confidence intervals when
\code{ci_lower}/\code{ci_upper} columns are present.}

\item{facet}{Logical; if \code{TRUE}, facet by metric.}

\item{sort}{Logical; if \code{TRUE}, order comparisons by their mean plotted
value.}
}
\value{
A \pkg{ggplot2} plot object.
}
\description{
Visualizes the output of \code{phontrast()} with \pkg{ggplot2}.
The input may be either wide or long output. By default, values are plotted on
a separation-oriented scale so overlap-oriented metrics are transformed as
\code{1 - estimate}.
}
\examples{
set.seed(2026)
vowels <- data.frame(
  speaker = rep(c("s01", "s02"), each = 60),
  vowel = rep(rep(c("ih", "eh"), each = 30), 2),
  f1 = c(rnorm(30, 500, 55), rnorm(30, 560, 60),
         rnorm(30, 510, 60), rnorm(30, 575, 65)),
  f2 = c(rnorm(30, 1980, 150), rnorm(30, 1880, 155),
         rnorm(30, 1960, 160), rnorm(30, 1840, 165))
)

metrics <- phontrast(
  vowels,
  features = c("f1", "f2"),
  category_col = "vowel",
  group_col = "speaker",
  output = "long"
)

if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot_overlap_metrics(metrics)
}
}
