% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/percent_overlap.R
\name{estimate_overlap}
\alias{estimate_overlap}
\title{Estimate proportional overlap globally or by group}
\usage{
estimate_overlap(
  data,
  features,
  category_col,
  group_col = NULL,
  min_tokens = 20,
  bw = c("Hpi", "Hscv", "Hpi.diag", "scott.diag"),
  eval_on = c("pooled", "group1", "group2", "pooled_sample"),
  eval_n = NULL,
  eval_seed = NULL,
  engine = c("ks", "fast_diag", "fast_diagonal"),
  chunk_size = 1000L,
  method = c("mc", "legacy"),
  ...
)
}
\arguments{
\item{data}{Data frame with at least \code{category_col} and \code{features}.}

\item{features}{Character vector of feature column names (e.g., c("F1","F2")).}

\item{category_col}{Name of the column giving the two-way category factor.}

\item{group_col}{Optional character vector of one or more grouping columns.
If provided, returns per-group overlap. Multiple grouping columns are
combined into a labeled \code{group} value such as
\code{"Sex=F | Style=read"}.}

\item{min_tokens}{Minimum total tokens required (globally or per group).}

\item{bw}{Bandwidth selection method passed to \code{percent_overlap_kde()}.}

\item{eval_on}{KDE evaluation points passed to \code{percent_overlap_kde()}.}

\item{eval_n}{Optional maximum number of KDE evaluation points.}

\item{eval_seed}{Optional integer seed for KDE evaluation-point subsampling.}

\item{engine}{KDE evaluation engine passed to \code{percent_overlap_kde()}.
\code{"fast_diagonal"} is accepted as an alias for \code{"fast_diag"}.}

\item{chunk_size}{Chunk size for \code{engine = "fast_diag"}.}

\item{method}{Estimator passed to \code{percent_overlap_kde()}: \code{"mc"}
(default) or \code{"legacy"} (pre-1.1.0 self-normalized estimate).}

\item{...}{Additional arguments passed to \code{percent_overlap_kde()}.}
}
\value{
A tibble (global = one row; grouped = one per group) with
\code{overlap} as a 0--1 proportion.
}
\description{
Unified front-end for KDE-based proportional overlap between two categories.
The returned \code{overlap} column is a 0--1 proportion, not a 0--100
percentage.
}
