% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jsd_wrappers.R
\name{estimate_jsd}
\alias{estimate_jsd}
\title{Estimate Jensen–Shannon divergence or distance between two categories}
\usage{
estimate_jsd(
  data,
  features,
  category_col,
  group_col = NULL,
  do_boot = FALSE,
  n_boot = 1000,
  min_tokens = 5,
  est_distance = FALSE,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{data}{Data frame with at least \code{category_col} and \code{features}.}

\item{features}{Character vector of feature column names (e.g., c("F1","F2")).}

\item{category_col}{Name of the column giving the two-way category factor.}

\item{group_col}{Optional grouping column. If provided, returns per-group JSD.}

\item{do_boot}{Logical; if TRUE, run nonparametric bootstrap.}

\item{n_boot}{Number of bootstrap resamples.}

\item{min_tokens}{Minimum total tokens required (globally or per group).}

\item{est_distance}{Logical; if TRUE, return Jensen–Shannon \emph{distance} (sqrt of divergence).}

\item{conf_level}{Confidence level for bootstrap interval.}

\item{...}{Additional arguments passed to \code{jsd_kde_nd()}.}
}
\value{
A tibble. Global: one row with columns
scope, n_tokens, n_boot, jsd_point, jsd_mean, jsd_sd, jsd_low, jsd_high.
Grouped: one row per group with columns
scope, group, n_tokens, n_boot, jsd_point, jsd_mean, jsd_sd, jsd_low, jsd_high.
}
\description{
Use this function when Jensen-Shannon divergence (JSD) or Jensen-Shannon
distance is the primary outcome. If you want to compare JSD with Pillai,
Bhattacharyya, Mahalanobis, and percent-overlap metrics, start with
\code{compare_overlap_metrics()} instead.
}
\details{
JSD is bounded from 0 to 1 for two equally weighted distributions. Larger
values indicate greater category separation. Jensen-Shannon distance is
\code{sqrt(JSD)} and has the same direction of interpretation.
}
