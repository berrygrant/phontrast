% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speaker_jsd.R
\name{boot_jsd}
\alias{boot_jsd}
\title{Bootstrap JSD for each group}
\usage{
boot_jsd(
  data,
  group_col,
  category_col,
  features,
  n_boot = 300,
  min_tokens = 30,
  est_distance = FALSE,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{data}{Data frame containing acoustic measurements.}

\item{group_col}{String: name of column giving the grouping unit
(e.g., "speaker").}

\item{category_col}{String: name of column giving the category
to compare (e.g., "vowel"). Each group must have exactly two categories.}

\item{features}{Character vector of column names giving the acoustic space.}

\item{n_boot}{Number of bootstrap resamples per group.}

\item{min_tokens}{Minimum number of tokens per group required to compute
JSD. Groups with fewer tokens are dropped.}

\item{est_distance}{Logical; if TRUE, return Jensen–Shannon distance
(sqrt of divergence) instead of divergence.}

\item{conf_level}{Confidence level for bootstrap intervals.}

\item{...}{Additional arguments passed to \code{jsd_kde_nd()}.}
}
\value{
A tibble with one row per group and columns:
\code{group}, \code{n_tokens}, \code{n_boot}, \code{jsd_mean},
\code{jsd_sd}, \code{jsd_low}, and \code{jsd_high}.
}
\description{
Computes bootstrap mean, SD, and confidence interval for JSD within each
group (e.g., speaker), using resampling with replacement.
}
